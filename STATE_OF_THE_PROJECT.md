# STATE OF THE PROJECT — QUANT-PORTAL

> **Document VIVANT** — mis à jour par DIFF (une ligne par item + preuve),
> jamais réécrit de zéro à chaque session. Si vous ajoutez un item, ajoutez
> la preuve (commit, test, log) dans la même modification.
> Les 12 anciens documents d'audit/vision sont archivés dans
> `docs/archive/audits-2026-08/` (historique conservé, plus aucune ambiguïté
> sur « quel document fait autorité » : c'est celui-ci).

Dernière mise à jour : 2026-08-20

## 🎓 Principes institutionnels appliqués (savoir public de référence)

| Principe | Source publique | Application dans QUANT-PORTAL |
|---|---|---|
| **Meta-labeling** : séparer la direction (side) du sizing (taille) — un modèle secondaire filtre les faux positifs et pilote la taille | López de Prado, *Advances in Financial Machine Learning* (2018) ; [Wikipedia Meta-Labeling](https://en.wikipedia.org/wiki/Meta-Labeling) | `meta_label_filter()` : filtre les signaux faibles (en DEMO : réduit la taille ; en REAL : bloque) — la direction vient du méta-moteur, la taille du Kelly + pipeline |
| **Fractional Kelly** : le Kelly plein maximise la croissance log mais produit des drawdowns sévères ; ½-¼ Kelly sacrifie ~25-56 % de croissance pour réduire massivement la volatilité | Thorp (2011), MacLean et al. (2010) ; pratiques professionnelles (Ernie Chan, AQR) | `KELLY_FRACTION = 0.15` (≈¼ Kelly) + win rate plancher/plafond 0.45-0.65 (estimation d'edge incertaine → réduire) |
| **Risk parity** : équilibrer le RISQUE entre classes d'actifs, pas le capital | Bridgewater All Weather (1996/2012) ; Bob Prince, "Risk Parity Is About Balance" | `risk_parity_weights()` dans `MetaAllocationEngine` + corrélation des signaux (P1-10) pour ne pas concentrer sur 2-3 facteurs latents |
| **Volatility targeting** : maintenir une volatilité cible constante, taille = cible/vol réalisée | Pratique institutionnelle (Man AHL, AQR) ; Kelly continu f* = μ/σ² | `volatility_scale_factor()` (cible tick 0.04 %, bornes 0.25-2.0) |
| **Base currency (multi-devise)** : le portefeuille se mesure dans UNE devise de référence ; chaque valeur étrangère convertie au taux du jour | IFRS (spot rate au jour de transaction) ; pratiques comptables multi-devises | `core/fx.py` : devise de compte configurable, conversion réelle er-api, affichage honnête si FX indisponible |
| **Triple-barrier labeling** : les sorties réelles (SL/TP/temps) définissent le label, pas un horizon fixe | López de Prado, AFML ch. 3 | `MetaLabelingTripleBarrier` (modèles/lopez_de_prado.py) — utilisé pour l'apprentissage |
| **Drawdown management** : circuit breakers quotidiens + lifetime, drawdowns par taille de compte | Pratique institutionnelle (risque de ruine) | `RiskManager.check_circuit_breaker()` : 2,5-18 %/jour, 8-35 % lifetime selon le capital |
| **Déflated Sharpe Ratio (DSR)** : corriger le Sharpe de la sélection/fouille de données | López de Prado, AFML ch. 8 | `calculate_deflated_sharpe_ratio` — promotion challenger OOS + attribution PnL (P1-11) |
| **Risk budgeting conditionnel au régime** : le budget de risque et la taille des paris s'adaptent à l'état du marché (régime), dans des bornes dures — on ne parie jamais plus de 1.25x la config de base, on resserre les stops en régime défensif | Pratique institutionnelle (Bridgewater regime-based allocation, risk parity conditionnelle) ; voir aussi F2 de l'audit | LOT B : `core/regime_autonomy.py` (facteur lissé EMA + confiance HMM, bornes [0.60, 1.25]) appliqué par `RiskManager` (Kelly, plafond par actif, drawdowns jamais élargis) |

> Ces principes sont des références publiques de qualité ; ils sont appliqués de façon critique :
> ce qui est robuste (fractional Kelly, DSR, triple-barrier) est en production ; ce qui est
> théorique (Kelly plein, optimisations fragiles) est volontairement écarté ou borné.
 · Repo : `trad` · Langue : français

---

## 0. Preuves reproductibles (commandes)

| Preuve | Commande | Résultat au 2026-08-20 |
|---|---|---|
| Tests | `python -m pytest tests/ -q` | **459 passed** (~34 s) |
| Safety check | `python automated_production_safety_check.py` | ALL DEEP EDGE-CASE CHECKS SUCCESSFUL |
| Couverture cœur (P1-15) | `python scripts/check_coverage.py` | main 34 % / engine 84 % / risk_pipeline 98 % (seuils 30/80/95) |
| CVE (P1-9) | `python scripts/check_vulnerabilities.py` | 0 vulnérabilité haute/critique (80 dépendances) |

---

## 1. ✅ FAIT — items de l'audit indépendant (preuves)

| Item | Ce qui a été fait | Preuve |
|---|---|---|
| P0-1 | Fausse allégation « MevShield On-Chain active » retirée ; messages DEX-CEX honnêtes (signal-only / broadcast) | commit `58a0060` · test `test_lot1_p0_audit.py` |
| P0-2 | AUTH forcée automatiquement sur déploiement non-local (PORT/RAILWAY_*) ; bypass local DEMO préservé | `58a0060` |
| P0-3 | Mot de passe admin auto-généré JAMAIS loggé : livré par Telegram DM ou fichier 0600 | `58a0060` |
| P0-4 | Instrumentation `final_scale` (p10/p50/p90, persistance 5 min, facteur limitant) + **plancher anti-empilement 15 %** (0,8¹⁵≈3,5 % éliminé) | `d23144d`, `765abfb`, `f2aac61`, `1200d8b` · `test_lot2_p0_audit.py` |
| P0-5 | Scripts CLI alignés sur le live (hidden_dim 24, défaut constructeur 24, garde-fou `bias_audit` avec rejet) + **données 100 % réelles** (plus aucun fallback synthétique) | `d23144d`, `765abfb` · `run_*.py` |
| P1-7 | Découpage de `main.py` : **6136 → 4892 lignes** (59 routes API → `api/routes.py`, 6 schedulers → `schedulers.py`, extraction AST reproductible) | `3fdab5d` · `test_lot7_split.py` |
| P1-8 | `core/config.py` branché : **3 → 11 fichiers** (portefeuille, contrepartie, coûts, order flow, vol targeting, CVaR, drawdowns, multi-source) | `8f6b010` · `test_lot3_config.py` |
| P1-9 | CI sur `requirements.lock` + `requirements-dev.txt`, Python 3.11 aligné prod, **pip-audit bloquant** sur CVE haute/critique | `c4b3926` · `test_lot4_ci.py` |
| P1-10 | Corrélation des SIGNAUX inter-stratégies (facteur max(0,5 ; 1−corr)) — l'angle mort §4.4 est fermé | `30eb80f` · `test_lot5_engine_audit.py` |
| P1-11 | `update_pnl_attribution` par **Sharpe déflaté** (López de Prado) + plancher d'échantillon (±20 % sous 20 trades) | `30eb80f` |
| P1-12 | Bandit Thompson : **oubli 0,98** (non-stationnaire) + **tirage figé** par cycle de décision (60 s) | `30eb80f` |
| P1-13 | Slippage live par **book-walking** du carnet (sizing + SOR par venue) | `b3a57e5` · `test_lot6_slippage_exposure.py` |
| P1-14 | `max_exposure_normal` **0,25 → 0,75** (vs 0,25 par actif) ET branché réellement (constante morte corrigée) | `b3a57e5` |
| P1-15 | Seuils de couverture **par fichier** (main 30 / engine 80 / risk_pipeline 95) | `c4b3926` · `test_lot4_ci.py` |
| P2-16 | `market_data/macro_calendar.py` → shim de dépréciation vers `models/macro_calendar.py` (plus de FOMC simulé) | `b792eb3` · `test_lot8_cleanup.py` |
| P2-17 | `models/telegram_bot.py` → `bot/telegram_bot.py` (28 Ko, pas un modèle quantitatif) | `b792eb3` |
| P2-18 | Shim `db_manager.py` racine supprimé ; 5 importeurs → `database.db_manager` | `b792eb3` |
| P2-19 | **Violation réelle corrigée** : RLHF (ÉDUCATIF) alimentait le sizing → `rlhf_scale=1.0` constant, module non chargé en prod + garde-fou mécanique (mapping facteur→module, spy AST) | `fe43fe8` · `test_lot9_educational.py` |
| P2-20 | Ce document + archive des 12 documents d'audit | commit `643b106` · `test_lot10_state.py` |

### Fixes supplémentaires (hors liste P0/P1/P2)

| Fix | Détail | Preuve |
|---|---|---|
| VPIN aberrant (6 988 465) | Bug racine `bucket_size_volume` fixe → buckets de volume égal, VPIN borné [0,1] ; modulate de conviction sécurisé (hors [0,1] = neutre) | `1200d8b` · `test_lot2_p0_audit.py` |
| Fidélité DEMO == REAL | SOR multi-venue exécuté en DEMO + `simulate_paper_fill` TOUJOURS book-walké (avant : prix fixe ±3 bps si carnet présent) | `3fdab5d` · `test_lot7_split.py` |
| Collecte final_scale | Persistance DB (survit aux redémarrages, perte max 5 min), endpoint `/api/v1/final-scale`, `/api/v1/paper-validation` | `765abfb`, `f2aac61` |
| Pollution DB par les tests | Tests mockent la DB (leçon : le p50=11,45 % initial était des données de test) | `f2aac61` |
| Ruff réparé et bloquant | `W503` retiré (règle supprimée de ruff), ~2000 corrections sûres, 3 vrais bugs F821 corrigés (stress test du scheduler autonome ne s'exécutait JAMAIS), **`ruff check .` = 0 erreur**, CI bloquant | `359c13c` · `test_lot4_ci.py` |
| Test flaky corrigé | `test_meta_allocation_dominance` figé via cache de tirage Thompson (8/8 vertes, avant ~40 % d'échec) | `359c13c` |
| Warning MLOps éliminé | **Vrai bug trouvé** : `get_setting(key, "défaut")` passait le défaut en `user_id` → `int("")` ; appels corrigés + test de régression | `359c13c` · `test_lot9_educational.py` |
| Contention SQLite réglée | Tests isolés sur `tests/test_trading.db` (base fraîche par session, `.gitignore`) — pytest peut tourner pendant que le serveur écrit | `359c13c` · `tests/conftest.py` |
| Étape 2 du découpage | `telemetry.py` extrait (`serialize_helper`, `compile_telemetry_data`, `broadcast_telemetry`) — main.py 4918 → 4696 lignes | `359c13c` |
| Fix prod (logs Railway) | `/api/v1/health` 500 corrigé (imports directs des symboles perdus par le nettoyage ruff F401 — `compute_health_score` etc.) ; supervisor silencieux en pause volontaire ; **test : toutes les routes GET répondent sans NameError** | `ab251aa` · `test_routes_health.py` |
| Mini-app fiable (mandat) | **Cause trouvée** : pas de polling + état initial 100 % simulé (chiffres fictifs affichés !). Corrigé : polling REST 5 s (parité fallback dashboard), état initial honnête (—/chargement), indicateur de fraîcheur 🟢/🟡/🔴, erreurs visibles. Tests verrouillés | commit mandat · `test_miniapp_50usd.py` |
| Accès marchés vérifié | Crypto (Coinbase/Kraken/OKX/CoinGecko), Or XAUUSD (Yahoo GC=F + gold-api), Forex EURUSD (Yahoo + er-api), Actions AAPL/TSLA (Yahoo) — toutes les classes d'actifs alimentées par des sources RÉELLES (Binance/Bybit géobloqués en sandbox, OK en prod) | test live 2026-08-20 |
| Multi-devise (compte) | Devise de compte configurable (`account.currency` : USD/EUR/GBP/JPY/CHF/...) + conversion RÉELLE (open.er-api.com, cache 5 min) de balance/équité/PnL dans la devise du compte (principe base currency IFRS/GAAP) ; l'interne reste en USD ; UI mini-app + dashboard affichent la devise ; FX indisponible -> affichage USD honnête (jamais de taux inventé) | commit multi-devise · `test_multicurrency.py` (11 tests) |
| Intelligence Axe 1 | Conviction CALIBRÉE par le win rate réel (`calibrated_conviction`, interpolation 0.45→x0.60 … 0.65→x1.25, bornée, neutre sans historique) — le sizing reflète la probabilité calibrée de succès (meta-labeling, López de Prado) au lieu de \|signal\| brut | `18909c6` · `test_intelligence.py` |
| Intelligence Axe 2 | `RegimeSwitchingAllocator` ADAPTATIF : poids statiques = a priori, performance RÉELLE par (régime, stratégie) ajuste en ligne (EMA 0.2, min 5 obs, shift borné ±30 %) — non-stationnarité sans sur-réaction au bruit | `18909c6` |
| Intelligence Axe 3 | Risk budget CONDITIONNEL au régime : `regime_risk_scale` (Bear High Vol ×0.70, Erratic ×0.80, calme ×1.0, inconnu neutre) intégré à `total_risk_budget`/`rebalance` | `18909c6` |
| LOT A (F1 conviction) | Plancher anti-empilement **0.15 → 0.25** (signaux calibrés, risque porté par Kelly/CVaR/drawdowns) ; seuil de conviction **adaptatif à la distribution réelle** (base = p25 des \|signaux\| — avant : constante 0.08 qui bornait le seuil à [0.08, 0.14]) ; `active_factors` exposé (transparence de l'empilement). Preuve live : seuil 0.08 → 0.26, échantillons récents à 0.250 | `cf9b25d` · `test_lot_a_conviction.py` |
| Trading 50 $ prouvé | 44 ordres FILLED sur la session (Grid Trading SOL, allers-retours ~20 s, ~3 $/trade — le min-notional remonté) ; l'impression « ne trade pas » vient de la petitesse/rapidité + NO_TRADE fréquents (régime Bear Trend High Vol, prudence par design) | journal DB 2026-08-20 |
| LOT B (F2 autonomie) | Auto-adaptation BORNÉE des paramètres de risque au régime HMM : `core/regime_autonomy.py` — mapping {0: 1.10, 1: 0.75, 2: 1.00, 3: 0.85}, **bornes dures [0.60, 1.25]** (jamais > 1.25x), **pull par confiance HMM** (régime incertain → facteur vers 1.0), **lissage EMA** (α=0.30, pas de saut sur un tick bruité), **drawdowns JAMAIS élargis** (×min(f,1), planchers 1.5 %/5 %). Appliqué par `RiskManager.effective_params()` (Kelly, plafond par actif, circuit breakers) + `max_asset_qty` live. DÉMO == RÉAL (aucun flag de mode). Télémétrie `regime_autonomy` (facteur lissé, cible, bornes, effectifs). Preuve live : régime Bull conf. 1.0 → facteur 1.0865, Kelly 0.15→0.163, plafond 0.25→0.2716, drawdowns micro 0.18/0.35 intacts | commit LOT B · `test_lot_b_autonomy.py` (29 tests) |
| LOT C (F3 architecture) | **main.py 4788 → 3959 lignes** (< 4000 ✓) : 5 blocs extraits par AST (corps inchangés) vers `core/observability.py` (final_scale P0-4 + paper-validation P0-6, 240 l.), `core/ccxt_client.py` (58 l.), `market_data/historical_fetch.py` (fetch OHLCV réels Binance/Bybit/Yahoo, 144 l.), `core/autonomous_ai.py` (scheduler LOT 66, 328 l.), `core/decision_explain.py` (54 l.). **FIN des 3 `from main import *`** (api/routes.py, schedulers.py, telemetry.py) → imports EXPLICITES vérifiés par AST ; les modules extraits importent depuis leurs VRAIES sources (core.self_assessment, core.world_model, models.lopez_de_prado, strategies.engine...) sauf les instances d'état (STATE/db/...) qui restent partagées via main (pattern documenté, test verrouillé). Ré-exports main.X préservés (tests, TASK_FACTORIES, routes). Même canal de logs (InstitutionalTradingBot). Preuve live : serveur relancé, 662 échantillons final_scale persistés chargés, payload télémétrie 102 clés | commit LOT C · `test_lot_c_architecture.py` (9 tests) |
| LOT 10 (mandat — Paper-Trading) | **`core/paper_validation.py`** (nouveau) : rapport OBJECTIF de préparation au REAL — 10 critères PASS/WARN/FAIL, chacun avec preuve (données réelles DB + STATE) : C1 CONTINUITÉ (jours datés P0-6, streak, trous), C2 FONCTIONNEMENT (décisions/fills/heartbeat), C3 COHÉRENCE (erreurs critiques), C4 PROTECTION (drawdown max, kill switch, état risque), C5 EXPLICABILITÉ (% décisions avec raison + non-trades catégorisés), C6 LIMITES (taux rejets OMS), C7 NON-DÉRIVE (drift PSI + edge decay), C8 EXÉCUTION (slippage moyen, forecast error), C9 CALIBRATION (buckets conviction, calibration error), C10 PNL NET (après coûts, trades clôturés ≥ 30 pour significativité). **Statut global READY / IN_PROGRESS / NOT_READY** — la décision de passage en REAL reste MANUELLE (note explicite dans le rapport, jamais automatique). API `/api/v1/paper-validation-report` + télémétrie `paper_validation_report` + dashboard (section validation). **Preuve live (rapport réel)** : **NOT_READY** — 5 PASS (fonctionnement 1261 décisions/890 fills, cohérence, protection drawdown 0 % + kill switch inactif, explicabilité 100 %, limites rejets 0 %) · 4 WARN (dérive PSI SEVERE — le système s'adapte, exécution/calibration/PnL : 0 clôtures < 30 — échantillon insuffisant) · **1 FAIL : continuité 3/28 jours** — la validation ne peut PAS être déclarée avant preuve réelle (le mandat : « ne jamais déclarer la validation avant d'avoir une preuve ») | commit LOT 10 · `test_lot10_paper_validation.py` (18 tests) |
| LOT 9 (mandat — Résilience / Gouvernance) | **`core/system_version.py`** (nouveau) : gouvernance §20 — `config_hash` (SHA-256 de config.yaml, change si la config change — testé), `git_commit` (best-effort), `system_version` composite `qp-{commit}-{hash[:8]}`, `deployed_models` (registre MLOps réel : settings model_status_*_DEPLOYED), `decision_version()` injecté dans CHAQUE décision. **Decision journal versionné** : colonnes `system_version`/`config_hash` ajoutées (migration ALTER TABLE pour bases existantes — SQLite/Postgres) + `journal_decision` les enregistre → « avec quelle version cette décision a été prise » est rejouable. **API `/api/v1/governance`** (version, modèles déployés, état du snapshot recovery, dernière réconciliation, audit trail) + télémétrie `governance`. **Résilience existante VÉRIFIÉE (non réimplémentée)** : snapshots d'état save/restore (2h max age, testé), ReconciliationEngine (mismatch balance/positions/orders/fills → retour False → HALT, testé avec le CONTRAT RÉEL — False = mismatch, True = aligné), audit_logs chaînés SHA-256, backups quotidiens, watchdog + redémarrage des tâches. **Preuve live** : system_version `qp-2351a5f-0419a7b1`, modèles déployés BTCUSDT {hmm: v_hmm_1787259527, lstm: v_lstm_1787092542}, décisions 1120/1121 du journal avec version + hash enregistrés | commit LOT 9 · `test_lot9_governance.py` (16 tests) |
| LOT 8 (mandat — Architecture) | **main.py 3998 → 3716 lignes** (réduction −282) : ① **`core/state_helpers.py`** (extrait par AST, corps inchangés) : set_data_quality, set_asset_quality, _neutral, record_open_position, mark_real_price, update_asset_order_book — dépendances : STATE/DataQualityStatus/platform_metrics/order_flow ; ② **`core/auth_helpers.py`** : _is_remote_deployment, auth_enforced, require_auth, require_admin, _telegram_send_sync, _deliver_admin_password_once, _ensure_auth_secrets — ré-exports compatibles dans main (api/routes, telemetry, schedulers inchangés — interface main.X stable, testée) ; **BUG détecté et corrigé en cours d'extraction** : la suppression par AST a emporté `from database.auth import AuthManager, Roles` (et ruff --fix retirait ensuite le ré-export F401) → ré-export explicite `# noqa: F401` requis (routes.py importe AuthManager depuis main). ③ **`core/brains.py`** : REGISTRE DES 8 CERVEAUX (mandat §17) — MARKET (9 modules), ALPHA (7), CONVICTION (3), DECISION (4), RISK (9), EXECUTION (6), LEARNING (5), ANALYST (7) ; 48 modules répertoriés ; vérifications exécutables : tous importables, AUCUN `from main import *` dans le repo (découplage par AST). API `/api/v1/brains` + télémétrie `brains`. **Preuve live** : 8 cerveaux, 48 modules, checks all_importable=true + no_star_import=true | commit LOT 8 · `test_lot8_architecture.py` (14 tests) |
| LOT 7 (mandat — Execution Intelligence) | **`core/execution_intel.py`** (nouveau) : consolidation post-trade de la QUALITÉ D'EXÉCUTION — ① **Implementation Shortfall** (IS bps adverse > 0 + IS $) sur chaque fill ; ② **COMPARAISON PRÉVISION vs RÉALITÉ** : `forecast_error = realized − expected` (SlippageModel) — un modèle d'exécution qui sous-estime systématiquement le slippage devient VISIBLE ; ③ **Venue quality** par venue (n, avg IS, latence moyenne, fill ratio — None si non mesurés, jamais inventés) ; ④ persistance best-effort `db.add_event("exec_intel")` (journal ré-écoutable). **Branchements main.py (3998 lignes)** : `execution_intel.record(...)` au fill RÉEL (arrival=_arrival_price, expected=SlippageModel avant update) ET au fill PAPIER (latence + slippage du simulateur — DEMO == REAL). Télémétrie `execution_intel` (by_venue, by_style, forecast error global) + API `/api/v1/execution-intel`. **Contexte déjà en place (non réimplémenté)** : choix de style (decide_style market/limit/twap + bandit epsilon), SOR multi-venue au coût net (pick_best_venue_net + book-walking), SlippageModel recalibré, Almgren-Chriss, simulateur. **Preuve live** : n=0 au démarrage (aucun fill dans la fenêtre — rapport honnête, rien inventé) ; convention IS alignée sur ExecutionAlpha (testée) | commit LOT 7 · `test_lot7_execution_intel.py` (15 tests) |
| LOT 6 (mandat — Validation) | **Adversarial Decision Engine** (`core/adversarial_engine.py`, nouveau) : teste CHAQUE décision avant exécution sous 9 scénarios d'échec — **exécution** : spread ×2, slippage ×2/×3, latence ×5 (dérive prix), liquidité ÷2 ; **extrêmes** : vol ×2 (stop traversé), signal inversé (perte = SL), gap ×1.5, données incohérentes. Calculs : baseline = edge net × SL ; **worst case** = min des scénarios ; **espérance stressée** = moyenne des scénarios d'EXÉCUTION uniquement (les extrêmes pèsent dans le risque de rupture, pas dans l'espérance — calibrage mesuré : avec la moyenne de tous, TOUT trade devenait FRAGILE, le bot n'aurait plus jamais tradé) ; FRAGILE si worst < −5 % (max_loss) OU espérance d'exécution ≤ 0 (« un trade qui n'est rentable que dans un environnement parfait est fragile »). **Défensif** : edge inconnu → ROBUST (pas de blocage sur info absente — protection additive). **Branchement** : `TradeOpportunityEngine` reçoit `adversarial` — mode **block** (défaut) → WAIT `ADVERSARIAL_FRAGILE` ; mode warn → signalé, trade passe. main.py (3998 lignes) : calcul adversarial (SL réel du trade) avant la décision. Télémétrie `adversarial` + API `/api/v1/adversarial`. Config `adversarial.*` (7 clés). **Validation existante VÉRIFIÉE (non réimplémentée)** : walk-forward + OOS + DSR (scheduler autonome), `audit_backtest` (look-ahead/survivorship/slippage → REJECTED), Monte Carlo stress, bootstrap Sharpe, stress crises réelles — les garde-fous anti-biais sont couverts par tests. **Preuve live** : verdict ROBUST, trade passé (26 TRADE / 444 WAIT cumulés), worst −1.6 %, slippage attendu indisponible → scénarios exécution au baseline (donnée absente = pas de pénalité inventée) | commit LOT 6 · `test_lot6_validation.py` (20 tests) |
| LOT 5 (mandat — Meta-Allocator hiérarchique) | **`core/hierarchical_allocator.py`** (nouveau) : ① **FAMILLES** (niveau 2) — `FAMILY_OF` : trend (Trend/Momentum/Cross-Sectional/Multi-Timeframe/VolBreakout), meanrev (MeanReversion/Grid), carry (MarketMaking/Carry), arbitrage (StatArb/InterExchange), micro (Scalping) ; ② **REGRET MINIMIZATION non-stationnaire** — `RegretTracker` : regret_i = decay×regret_i + max(0, best_pnl_récent − pnl_i) (écart INSTANTANÉ à la meilleure ex post, oubli exponentiel 0.98 — défaut de conception corrigé : l'écart CUMULÉ ne décroissait jamais, le regret doit mesurer l'instant, pas le retard historique) ; exploration proportionnelle au regret (regret matching, bornée 15 %) ; ③ **`HierarchicalAllocator.allocate`** : scale de famille par performance récente (EMA, borné [0.60, 1.20]) + exploration regret + bornes dures [0.02, 0.45] + renormalisation (somme = 1). **Branchements** : `MetaAllocationEngine` expose `regret_tracker` + `hierarchical_scales()` (record_regret appelé dans update_pnl_attribution — les clôtures alimentent le regret) ; `allocate(..., hierarchical_scales=...)` multiplie les poids (absent → comportement pré-LOT 5 STRICTEMENT identique, prouvé par test). Télémétrie `hierarchical_allocator` (regret + familles) + API `/api/v1/meta-allocator`. Config `hierarchical.*` (8 clés). **Preuve live** : allocation uniforme au démarrage (1/12 = 0.0833), scales famille 1.0, regret vide (pas encore de clôtures — aucun chiffre inventé) | commit LOT 5 · `test_lot5_hierarchical.py` (21 tests) |
| LOT 4 (mandat — Adaptativité : Edge Decay) | **`core/edge_decay.py`** (nouveau) : détection de la dégradation d'edge PAR STRATÉGIE avec cycle d'états **HEALTHY → DEGRADED → WARNING → DISABLED → RECOVERY**. Métriques : expectancy (EMA), hit rate (EMA), profit factor, coût moyen, performance par régime HMM. **Scales de pondération BORNÉS [0.30, 1.0]** — JAMAIS 0 (une stratégie dégradée est sous-pondérée, jamais arrêtée : DISABLED = observation à ×0.30) ; **RÉVERSIBLE** (RECOVERY dès que l'expectancy repasse positive — bug d'ordre corrigé : la remontée passait par DEGRADED donc `prev_state` n'était plus DISABLED → HEALTHY au lieu de RECOVERY). Pas de jugement sous 10 trades (n < min → HEALTHY, on n'invente pas une dégradation). **Branchements** : `record_closed_trade` → `edge_decay.record_outcome(strategy, pnl, régime)` ; `MetaAllocationEngine.allocate(..., edge_decay_scales={strategy: scale})` multiplie le poids final de chaque stratégie (absent → comportement pré-LOT 4 strictement identique, prouvé par test) ; les contributions exposent `edge_decay_scale` (télémétrie cohérente avec la décision). Télémétrie `edge_decay` + API `/api/v1/edge-decay`. Config `edge_decay.*` (10 clés). **Contexte déjà en place (non réimplémenté)** : drift PSI/CUSUM multi-actifs (LOT D, réaction INFO→SEVERE + oubli bandit), allocation par régime (RegimeSwitchingAllocator adaptatif + MoE + risk budgeting LOT B). **Preuve live** : 12 stratégies surveillées, toutes HEALTHY n=0 (aucun jugement sans échantillon — honnête) ; scales 1.0 ; les clôtures futures alimenteront le cycle | commit LOT 4 · `test_lot4_edge_decay.py` (22 tests) |
| LOT 3 (mandat — Trade Intelligence) | **Decision Journal + Trade Intelligence Database** : table `decision_journal` (database/db_manager.py — SQLite/Postgres, idempotente) avec colonnes structurées (ts, decision, symbol, regime, signal, conviction, level, edge_net, win_rate, reason, detail, threshold, risk_state, strategy, qty, price, slippage attendu/réel, pnl_pct, mfe/mae, duration, exit_reason, payload JSON) + `log_decision_entry`/`update_decision_outcome`/`get_decision_journal`/`decision_journal_summary` (jamais bloquants). **`core/decision_journal.py`** : `journal_decision` (1 ligne = 1 décision TRADE ou WAIT), `journal_fill` (complétion à l'exécution), `close_journal_entry` (finalisation à la clôture : pnl, durée, raison de sortie, MFE/MAE estimés sur candles réelles — None si non mesurable, jamais inventé), `non_trade_analysis`. **Câblage main.py (3997 lignes)** : journal_decision à la décision (avec id mémorisé par symbole), journal_fill au fill papier, close_journal_entry dans record_closed_trade. **API** : `/api/v1/decision-journal` (décisions + summary) et `/api/v1/non-trade`. Télémétrie : `decision_journal_summary`. **Preuves live** (1 min de fonctionnement) : 77 décisions — 4 TRADE / 73 WAIT, par raison : conviction 50, UNCALIBRATED 23, opportunity_ok 4 ; chaque WAIT avec détail explicite (« \|signal\| 0.056 < seuil 0.080 », « setup intéressant mais conviction non calibrée ») ; closed_n=0 (trades ouverts non clôturés — honnête) | commit LOT 3 · `test_lot3_trade_intel.py` (14 tests) |
| LOT 2 (mandat — Conviction Engine + Trade Opportunity/No-Trade) | **`core/conviction_engine.py`** (nouveau) : ① `expected_edge_net(p, R, coûts, SL)` = p·R − (1−p) − coûts/SL — espérance NETTE par unité de risque (None sans historique : on n'invente pas un edge) ; ② `ConvictionEngine.calibrate()` — enveloppe `calibrated_conviction` (base STRICTEMENT identique, zéro ré-implémentation) + modificateurs DÉFENSIFS bornés (régime incertain ×0.95, slippage >50 bps ×0.90, jamais de boost artificiel) + niveau HIGH/NORMAL/LOW/UNCERTAIN/NO_TRADE + edge net + uncalibrated ; ③ `track_outcome` + `calibration_report()` — calibration MESURÉE : buckets [0-0.08)…[0.40+), win rate observé par bucket, expectancy, calibration error (rien de fabriqué : n=0 → « en attente de données ») ; ④ `TradeOpportunityEngine.evaluate()` — décision TRADE/WAIT explicite, ordre fixe (HALT → seuil → EDGE_INSUFFICIENT → EXECUTION_RISK → UNCALIBRATED), raisons stables instrumentées (« edge net <= 0 après frais + slippage », « setup intéressant mais conviction non calibrée »). **Câblage main.py (3998 lignes)** : calibrate remplace l'appel brut dans le pipeline, evaluate remplace la décision seuil (repli pré-LOT 2 conservé), conviction mémorisée par trade à l'exécution, track_outcome à la clôture (record_closed_trade). Télémétrie : conviction_engine, last_opportunity, conviction_calibration. Buckets NO_TRADE étendus (edge_insufficient/uncalibrated/execution_risk). Config `conviction.*` (5 clés). **Preuves live** : signal 0.042 < seuil 0.08 → WAIT conviction avec détail explicite ; edge net +0.193 affiché ; level UNCERTAIN (pas d'historique) ; calibration n=0 honnête | commit LOT 2 · `test_lot2_conviction.py` (27 tests) |
| LOT 1 (mandat — diagnostic réel + observabilité décisionnelle) | **Diagnostic** (état réel vérifié dans le code, pas supposé) : ① la conviction a un seuil ADAPTATIF (p25 des |signaux|, plancher 0.08) et un calibrage par win rate (LOT A) — mais les signaux produits sont réellement faibles (preuve live : abs_p50=0.079, seuil au plancher 0.08) : la « quasi-silence » vient des SIGNAUX, pas du seuil ; ② méta-allocation (bandit non-stationnaire + DSR + MoE + régime) et risk budgeting (vol/CVaR/régime + LOT B) déjà adaptatifs ; ③ **BUGS RÉELS trouvés et corrigés** : (a) `decide_no_trade` incrémentait `no_trades` mais l'état/télémétrie lisent `count` → le compteur NO_TRADE affiché restait à 0 en permanence ; (b) le breakdown par catégorie échouait silencieusement (`x[k]=y` évalue y avant setdefault → KeyError avalée par try/except) ; (c) raison NO_TRADE par défaut sans |signal| vs seuil (32 % des 9051 abstentions historiques peu informatives) ; (d) cycle d'import pré-existant test_lot2 seul (observability avant main) fiabilisé. **Modifications** : `core/meta_cognition.py` (raison enrichie |signal| vs seuil + bucket par catégorie conviction/rr_filter/halt/order_flow/cascade/meta_label/regime_context/other), `telemetry.py` (no_trade_reasons + signal_stats exposés), dashboard (section « POURQUOI PAS DE TRADE ? » : seuil, compteur, breakdown), `test_lot2_p0_audit.py` (import main d'abord). **Preuves** : 654 tests (+17 `test_lot1_diagnostic.py`), ruff 0, live : no_trade_count=38 (n'est plus 0), breakdown conviction=38, signal_stats n=75 p50=0.079 | commit LOT 1 |
| LOT F (F6 autonomie opérationnelle) | **Alertes Telegram automatiques** : `core/ops_alerts.py` — `send_ops_alert` (dédupliquée par type, cooldown mémorisé dans STATE, `force=True` pour le critique), `drift_transition` (alerte sur TRANSITION d'état : STABLE/MODERATE→SEVERE « drift détecté », SEVERE→STABLE « drift résorbé » — jamais de spam sur état persistant), `circuit_breaker_text`/`drift_severe_text` (messages ACTIONNABLES avec référence runbook). **Câblage main.py** : alerte kill switch au trip du circuit breaker (avec nb de positions flatten) + alerte drift après chaque `run_drift_check` (transition, dédupliquée). Principes : jamais bloquant (erreur Telegram = log, le trading continue), silencieux sans config (mode alert-silent), DÉMO == RÉAL (aucun flag de mode). **RUNBOOK.md 85 → 151 lignes** : §7 DRIFT (symptômes, ce que fait le bot, procédure), §8 DRAWDOWN/KILL SWITCH (ne PAS réarmer sans diagnostic, procédure complète), §9 HALT (raisons, redémarrage progressif, reset manuel), §10 AUTONOMIE OPÉRATIONNELLE (tableau automatique vs opérateur : réglages auto par régime/drift/redémarrage, réarmement kill switch et passage REAL **jamais automatiques par conception**). Télémétrie : `ops_alerts_last_ts` exposé. **Audit réel** : le trou F6 = AUCUNE notification sur kill switch ni drift sévère (vérifié : seulement audit log + changement d'état risque) — corrigé ; le redémarrage HALT était déjà automatique (documenté, pas réécrit) | commit LOT F · `test_lot_f_ops.py` (24 tests) |
| LOT E (F5 modules éducatifs) | **Verrouillage RENFORCÉ** (LOT 9 → LOT E) : ① **Registre source unique** — `core/module_honesty.py` : `EDUCATIONAL_MODULES` canonique (rlhf, gan_scenarios, options_volatility, llm_narrative), `assert_registry_coherent()` (statuts valides, 4 éducatifs présents — 16 PROD / 4 EXP / 4 ÉDUC) ; ② **Spy AST REPO ENTIER** — les 4 racines d'imports éducatifs (`rl.rlhf_reward_model`, `ai.generative_extreme_scenarios`, `models.volatility_arbitrage`, `core.llm_narrative`) ne sont importées QUE par la liste blanche (main, autonomous_ai — stress GAN = jauge Prometheus uniquement, api/routes, schedulers, telemetry, modules eux-mêmes) et JAMAIS par le cœur décisionnel (risk_pipeline, sizing, exécution, allocation, stratégies — 24 fichiers vérifiés) ; ③ **Data-flow** — le bloc du pipeline dans main.py ne référence aucune sortie éducative (gan_stress_ratio, options_strategy, real_iv, generative_engine, volatility_arb_engine, rlhf_reward_model, llm_narrative) ; ④ **Safety check** — `Module_Registry_Locks` ajouté à automated_production_safety_check.py ; ⑤ **UI cohérente** — dashboard liste les modules éducatifs verrouillés (🔒 rlhf · gan_scenarios · options_volatility · llm_narrative), mini-app compteurs, `/api/v1/honesty` expose registre + summary. Audit réel : aucune fuite trouvée (rlhf_scale=1.0 constant, GAN = télémétrie, options = affichage, LLM = narrative) — le verrouillage est désormais MÉCANIQUE (tests + safety check) | commit LOT E · `test_lot_e_educational.py` (13 tests) |
| LOT D (F4 online learning) | **PSI (Population Stability Index)** sur les features clés : `core/drift_psi.py` — `compute_psi` (bucketing par percentiles de la référence), `DriftMonitor`, `run_drift_check` (tick périodique 15 min, jamais bloquant). **Oubli du bandit ACCÉLÉRÉ au drift** : `strategies/engine.py` — decay dynamique `set_bandit_decay()` (nominal 0.98 → 0.92 en sévère, borné [0.85, 0.995]) + paramètre `decay` dans `update_bandit_feedback`. **CORRECTIONS (tour LOT E) — mesurées sur données réelles, pas de la théorie** : ① **MULTI-ACTIFS** : `MultiAssetDriftMonitor` — PSI par actif sur les 7 (max global = pire cas, per_asset exposé) ; ② **CACHE PROFOND** : fetch 700 barres (Binance/Bybit limit=700, Yahoo range=6mo → 4312 barres crypto / 2881 or / 876 actions mesurés) + mise à niveau auto du cache au boot (seuil 250 → 2500 chargées pour le PSI) ; ③ **FUSION CUSUM + PSI** : `unified_drift_state` — SEVERE si l'un des deux est sévère, decay = le plus agressif ; le flag CUSUM (détection concept drift MLOps) accélère aussi le bandit (hold 1 h) ; ④ **CALIBRAGE** : les seuils crédit 0.10/0.25 → SEVERE PERMANENT sur données de marché (mesuré : 7/7 actifs) → seuils marché 0.30/0.60, fenêtres alignées sur l'horizon de régime (référence 2000 h ≈ 3 mois, récente 400 h ≈ 17 j), momentum_10 RETIRÉ (détecteur de tendance, pas de distribution — PSI 2-5x plus élevés mesurés), volume_log exposé mais non déclencheur (données inconstantes). Preuve live : BTC/ETH/SOL/XAU STABLE (decay 0.98), EURUSD MODERATE→SEVERE selon fenêtre (shift de moyenne réel, documenté), 7/7 actifs calculés (n_bars=2500) | commit LOT E · `test_lot_d_drift.py` (39 tests) |

---

## 2. 🔄 EN COURS / NON TERMINÉ (processus, pas du code)

| Item | État réel au 2026-08-20 | Ce qui manque |
|---|---|---|
| **P0-6** : paper-trading daté et CONTINU avant REAL | Tracker opérationnel (`/api/v1/paper-validation`) : **2 / 28 jours** marqués, `validated: false` | 26 jours de fonctionnement réel continu (le bot doit tourner) |
| Observation `final_scale` 24-48 h (P0-4 diagnostic) | Collecte en cours (serveur live, persistance 5 min) : p50 = 0,15 (plancher actif), **facteur limitant = conviction** (|signal| ~0,10) | 24-48 h de données continues ; décision sur le seuil de conviction à trancher sur ces données |
| Preuve book-walking / SOR-DEMO en conditions réelles | Mécanisme testé + simulé ; en attente de trades DEMO réels (le pipeline trade rarement, signaux faibles) | Logs `SOR_CHOICE_DEMO` / `book_slippage_bps` sur vrais trades |
| Découpage `main.py` | Étape 1 faite (extraction routes + schedulers, −1244 lignes) | Découplage fin des dépendances (`from main import *`) — optionnel |

---

## 3. ⚠️ LIMITES CONNUES / DETTE ASSUMÉE

| Limite | Détail | Statut |
|---|---|---|
| Torch absent du sandbox | GAN (LOT 54) et RLHF (LOT 55) en fallback neutre — cohérent avec l'étiquette ÉDUCATIF | Assumé |
| Flakiness `check_coverage.py` en sandbox | 2 tests intermittents (`test_routes_health`, `test_smoke`) : `sqlite3.OperationalError` = connexions DB non fermées sous charge + réseau lent (Yahoo 429 / Bybit 403) — pré-existant, reproduit sur HEAD sans LOT B (10 failed) ; la suite complète passe (554) et la couverture mesurée dépasse les seuils (main 56,8 % / engine 88,7 % / risk_pipeline 98,8 %) | Assumé — en CI GitHub Actions le run est fiable |
| `main.py` à 3 959 lignes | LOT C : 5 blocs extraits (< 4000 atteint) ; le cœur `live_trading_loop` (~1 850 l.) et les boucles WS restent dans main — extraction plus fine optionnelle | Assumé |
| `from main import (liste)` dans les modules extraits | Imports EXPLICITES (fini le `*`) ; les instances d'état (STATE/db/...) restent partagées via main — pattern documenté, test verrouillé | Découplage total des instances = chantier séparé (non planifié) |

---

## 4. Registre d'honnêteté (module_honesty)

- **PRODUCTION (16)** : multi_source_price, risk_pipeline, risk_state_machine, order_flow, execution_sor, position_lifecycle, portfolio_allocator, macro_calendar, sentiment, counterparty_risk, reconciliation, watchdog, cost_accounting, attribution, scenario_stress, bias_audit.
- **EXPÉRIMENTAL (4, gardés)** : regime_confidence, mlops_challenger, causal_discovery, meta_attribution.
- **ÉDUCATIF (4, JAMAIS dans le sizing — verrouillé par test P2-19)** : rlhf, gan_scenarios, options_volatility, llm_narrative.

---

## 5. Comment mettre à jour ce document

1. Une ligne par item, avec **preuve** (commit `xxxxxxx`, fichier de test, log réel).
2. Déplacer un item de « EN COURS » vers « FAIT » uniquement avec une preuve
   (test vert, log, chiffre) — jamais « parce que ça devrait marcher ».
3. Ne jamais ajouter de section de complétion globale : l'état réel du
   projet est celui de ce document, y compris ses limites.
